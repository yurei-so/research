[project]
name = "yurei-state-encoding"
version = "0.1.0"
description = "Typed machine-observation serialization apparatus"
license = "MIT"
requires-python = ">=3.11"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]
