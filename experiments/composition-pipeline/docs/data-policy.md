# Data and artifact policy

Do not commit prompts, generated model outputs, checkpoints, imported corpora,
credentials, or local experiment artifacts unless they have been deliberately
reviewed for publication.
