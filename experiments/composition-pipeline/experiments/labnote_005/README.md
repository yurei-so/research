# Labnote 005

This follow-up repeats the 120-assignment self-matched campaign with a modest
frozen temperature of 0.35 and unique repetition seeds. It content-hashes every
normalized unordered changed pair before intake. Identical outputs remain
automatic ties, and duplicate changed pairs contribute only multiplicity
telemetry. The owner sees one representative of each unique changed answer set.

The runner must report changed trials, unique review pairs, duplicate changed
trials, and the duplicate-group size distribution before the review surface is
opened. Private text, operations, hashes, mappings, and pair-level telemetry
remain owner-only.
