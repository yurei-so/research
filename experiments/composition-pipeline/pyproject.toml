[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "composition-pipeline"
version = "0.1.0"
description = "Experimental middleware for model-directed composition and revision."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
  { name = "Composition Pipeline contributors" }
]
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py311"
