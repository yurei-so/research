[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "conversation-prosody-pipeline"
version = "0.3.1"
description = "Middleware for enriching spoken conversations with measurable prosodic metadata."
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [
  { name = "Conversation Prosody Pipeline contributors" }
]
dependencies = []

[tool.ruff]
line-length = 100
target-version = "py311"
