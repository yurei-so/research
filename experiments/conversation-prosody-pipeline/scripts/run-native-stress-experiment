#!/usr/bin/env bash
set -euo pipefail

root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec "$root/.venv-kokoro/bin/python" \
  "$root/experiments/labnote_009/run_native_stress.py" \
  --source-run "$root/artifacts/labnote-004/oracle-synthesis" \
  --output-dir "$root/artifacts/labnote-009/native-stress-v1"
