# composition-009 shadow candidate

This directory is an experimental META-RESEARCH-001 observation. It does not
modify the canonical labnote or authorize public summary rendering.

- `candidate.json` is model-produced and must not be treated as evidence.
- `validator-result.json` records deterministic structural checks only.
- `human-review.json` remains incomplete until a human scores and decides on
  the candidate.

The candidate is not acceptable or promotable while the human review is
pending.
